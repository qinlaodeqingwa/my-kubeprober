# This rule is not used by the project my-kubeprober itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants permissions to create, update, and delete resources within the kubeprober.erda.cloud.
# This role is intended for users who need to manage these resources
# but should not control RBAC or manage permissions for others.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: my-kubeprober
    app.kubernetes.io/managed-by: kustomize
  name: probe-editor-role
rules:
- apiGroups:
  - kubeprober.erda.cloud
  resources:
  - probes
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - kubeprober.erda.cloud
  resources:
  - probes/status
  verbs:
  - get
